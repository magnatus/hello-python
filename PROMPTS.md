# Использованные AI-промты

Список промтов, использованных при разработке проекта с помощью Claude (claude-sonnet-4-6).

---

## Форма приветствия

**Промт:**
> Добавь в мой Flask-проект форму на главной странице, где пользователь вводит своё имя. После отправки формы показывается страница с приветствием "Привет, <имя>". Использовать POST-запрос, без базы данных.

**Результат:** маршрут `POST /`, шаблоны `index.html` и `greet.html`

---

## Архитектура мини-анкеты

**Промт:**
> Нужно сделать домашнее задание "Мини-анкета": GET /questions возвращает 3–5 фиксированных вопросов, POST /answers принимает ответы и хранит в памяти. Frontend загружает вопросы с backend, отображает их, отправляет ответы через POST, показывает "Спасибо!" после отправки. Не менять стек, не добавлять БД и React/Vue. Сначала предложи архитектуру и план без кода.

**Результат:** план из 4 шагов, структура файлов

---

## Backend анкеты

**Промт:**
> Давай сделаем шаг 1 — обнови app.py

**Результат:** маршруты `GET /api/questions` и `POST /api/answers`, список `QUESTIONS`, хранилище `answers`

---

## Frontend анкеты

**Промт:**
> Да, переходим к шагу 2

**Результат:** шаблон `questionnaire.html` с динамической формой на Vanilla JS и `fetch`

---

## Навигация

**Промт:**
> Переходим к шагу 3

**Результат:** ссылка «Анкета» в `base.html`

---

## Префикс /api для маршрутов

**Промт:**
> Лучше привести маршруты к более явной структуре API: GET /api/questions и POST /api/answers

**Результат:** переименованы маршруты в `app.py` и `fetch`-вызовы в `questionnaire.html`

---

## Валидация формы

**Промт:**
> Нужна базовая валидация в POST /answers — иначе "Спасибо" возвращает даже при пустых полях

**Результат:** проверка пустых полей на backend с возвратом `400`, отображение ошибки на frontend

---

## Стилизация через Tailwind CSS

**Промт:**
> Давай наведем красоту — можешь добавить небольшой React/Vue чтобы все страницы выглядели красиво

**Результат:** подключён Tailwind CSS через CDN, все шаблоны переверстаны с карточками, стилизованными инпутами и кнопками

---

## README

**Промт:**
> Помоги подготовить README.md: кратко описать проект, указать стек, перечислить API, дать инструкцию по локальному запуску, описать сценарий использования, добавить раздел про AI-промты.

**Результат:** файл `README.md`
